# Example configuration for the sample repository.
# This file deliberately contains a fake-but-realistic-looking credential so
# that contextcrucible's secret detector quarantines it in tests. The value is
